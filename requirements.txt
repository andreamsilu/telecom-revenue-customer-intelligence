# Runtime dependencies (Streamlit Community Cloud + local app).
# Dev tools (pytest, ruff, mypy) live in pyproject.toml optional [dev].

pandas>=2.2,<3.0
numpy>=2.0,<3.0
# PyArrow 22+ ships cp314 wheels; keep floor at 17 for older local envs.
pyarrow>=17.0,<26.0

streamlit>=1.40,<2.0
plotly>=5.24,<7.0

pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0
faker>=30.0,<40.0
